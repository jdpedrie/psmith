// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: spalt/v1/files.proto

package spaltv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jdpedrie/spalt/gen/spalt/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// FilesServiceName is the fully-qualified name of the FilesService service.
	FilesServiceName = "spalt.v1.FilesService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// FilesServiceUploadFileProcedure is the fully-qualified name of the FilesService's UploadFile RPC.
	FilesServiceUploadFileProcedure = "/spalt.v1.FilesService/UploadFile"
	// FilesServiceGetFileURLProcedure is the fully-qualified name of the FilesService's GetFileURL RPC.
	FilesServiceGetFileURLProcedure = "/spalt.v1.FilesService/GetFileURL"
	// FilesServiceListFilesProcedure is the fully-qualified name of the FilesService's ListFiles RPC.
	FilesServiceListFilesProcedure = "/spalt.v1.FilesService/ListFiles"
)

// FilesServiceClient is a client for the spalt.v1.FilesService service.
type FilesServiceClient interface {
	// UploadFile streams a single file's bytes to the server. The
	// client sends a Header message first (metadata), then one or more
	// Chunk messages (bytes), then closes the send side. The server
	// computes the SHA-256 as it goes, writes via the Storage backend,
	// and returns a single response with the resulting file_id.
	//
	// Idempotent on (user_id, sha256): re-uploading the same content
	// returns the existing file_id without rewriting the blob.
	//
	// Size cap: 50 MB enforced server-side (before any DB / Storage
	// writes). Larger uploads are rejected with InvalidArgument.
	UploadFile(context.Context) *connect.ClientStreamForClient[v1.UploadFileRequest, v1.UploadFileResponse]
	// Mint a short-lived signed URL for the file_id. The URL points at
	// the `/files/{id}?token=…` HTTP endpoint on the same spaltd and
	// is valid for ~30 seconds. Clients call this when they need to
	// render a thumbnail or trigger a download; the URL is HMAC-
	// signed so it can be passed to a system image-loader (or other
	// un-authenticated consumer) without leaking session credentials.
	GetFileURL(context.Context, *connect.Request[v1.GetFileURLRequest]) (*connect.Response[v1.GetFileURLResponse], error)
	// ListFiles returns recent files owned by the caller. Used by the
	// "uploaded files" sidebar / picker. Bytes are not included —
	// clients use GetFileURL to fetch any specific file's content.
	ListFiles(context.Context, *connect.Request[v1.ListFilesRequest]) (*connect.Response[v1.ListFilesResponse], error)
}

// NewFilesServiceClient constructs a client for the spalt.v1.FilesService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewFilesServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) FilesServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	filesServiceMethods := v1.File_spalt_v1_files_proto.Services().ByName("FilesService").Methods()
	return &filesServiceClient{
		uploadFile: connect.NewClient[v1.UploadFileRequest, v1.UploadFileResponse](
			httpClient,
			baseURL+FilesServiceUploadFileProcedure,
			connect.WithSchema(filesServiceMethods.ByName("UploadFile")),
			connect.WithClientOptions(opts...),
		),
		getFileURL: connect.NewClient[v1.GetFileURLRequest, v1.GetFileURLResponse](
			httpClient,
			baseURL+FilesServiceGetFileURLProcedure,
			connect.WithSchema(filesServiceMethods.ByName("GetFileURL")),
			connect.WithClientOptions(opts...),
		),
		listFiles: connect.NewClient[v1.ListFilesRequest, v1.ListFilesResponse](
			httpClient,
			baseURL+FilesServiceListFilesProcedure,
			connect.WithSchema(filesServiceMethods.ByName("ListFiles")),
			connect.WithClientOptions(opts...),
		),
	}
}

// filesServiceClient implements FilesServiceClient.
type filesServiceClient struct {
	uploadFile *connect.Client[v1.UploadFileRequest, v1.UploadFileResponse]
	getFileURL *connect.Client[v1.GetFileURLRequest, v1.GetFileURLResponse]
	listFiles  *connect.Client[v1.ListFilesRequest, v1.ListFilesResponse]
}

// UploadFile calls spalt.v1.FilesService.UploadFile.
func (c *filesServiceClient) UploadFile(ctx context.Context) *connect.ClientStreamForClient[v1.UploadFileRequest, v1.UploadFileResponse] {
	return c.uploadFile.CallClientStream(ctx)
}

// GetFileURL calls spalt.v1.FilesService.GetFileURL.
func (c *filesServiceClient) GetFileURL(ctx context.Context, req *connect.Request[v1.GetFileURLRequest]) (*connect.Response[v1.GetFileURLResponse], error) {
	return c.getFileURL.CallUnary(ctx, req)
}

// ListFiles calls spalt.v1.FilesService.ListFiles.
func (c *filesServiceClient) ListFiles(ctx context.Context, req *connect.Request[v1.ListFilesRequest]) (*connect.Response[v1.ListFilesResponse], error) {
	return c.listFiles.CallUnary(ctx, req)
}

// FilesServiceHandler is an implementation of the spalt.v1.FilesService service.
type FilesServiceHandler interface {
	// UploadFile streams a single file's bytes to the server. The
	// client sends a Header message first (metadata), then one or more
	// Chunk messages (bytes), then closes the send side. The server
	// computes the SHA-256 as it goes, writes via the Storage backend,
	// and returns a single response with the resulting file_id.
	//
	// Idempotent on (user_id, sha256): re-uploading the same content
	// returns the existing file_id without rewriting the blob.
	//
	// Size cap: 50 MB enforced server-side (before any DB / Storage
	// writes). Larger uploads are rejected with InvalidArgument.
	UploadFile(context.Context, *connect.ClientStream[v1.UploadFileRequest]) (*connect.Response[v1.UploadFileResponse], error)
	// Mint a short-lived signed URL for the file_id. The URL points at
	// the `/files/{id}?token=…` HTTP endpoint on the same spaltd and
	// is valid for ~30 seconds. Clients call this when they need to
	// render a thumbnail or trigger a download; the URL is HMAC-
	// signed so it can be passed to a system image-loader (or other
	// un-authenticated consumer) without leaking session credentials.
	GetFileURL(context.Context, *connect.Request[v1.GetFileURLRequest]) (*connect.Response[v1.GetFileURLResponse], error)
	// ListFiles returns recent files owned by the caller. Used by the
	// "uploaded files" sidebar / picker. Bytes are not included —
	// clients use GetFileURL to fetch any specific file's content.
	ListFiles(context.Context, *connect.Request[v1.ListFilesRequest]) (*connect.Response[v1.ListFilesResponse], error)
}

// NewFilesServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewFilesServiceHandler(svc FilesServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	filesServiceMethods := v1.File_spalt_v1_files_proto.Services().ByName("FilesService").Methods()
	filesServiceUploadFileHandler := connect.NewClientStreamHandler(
		FilesServiceUploadFileProcedure,
		svc.UploadFile,
		connect.WithSchema(filesServiceMethods.ByName("UploadFile")),
		connect.WithHandlerOptions(opts...),
	)
	filesServiceGetFileURLHandler := connect.NewUnaryHandler(
		FilesServiceGetFileURLProcedure,
		svc.GetFileURL,
		connect.WithSchema(filesServiceMethods.ByName("GetFileURL")),
		connect.WithHandlerOptions(opts...),
	)
	filesServiceListFilesHandler := connect.NewUnaryHandler(
		FilesServiceListFilesProcedure,
		svc.ListFiles,
		connect.WithSchema(filesServiceMethods.ByName("ListFiles")),
		connect.WithHandlerOptions(opts...),
	)
	return "/spalt.v1.FilesService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case FilesServiceUploadFileProcedure:
			filesServiceUploadFileHandler.ServeHTTP(w, r)
		case FilesServiceGetFileURLProcedure:
			filesServiceGetFileURLHandler.ServeHTTP(w, r)
		case FilesServiceListFilesProcedure:
			filesServiceListFilesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedFilesServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedFilesServiceHandler struct{}

func (UnimplementedFilesServiceHandler) UploadFile(context.Context, *connect.ClientStream[v1.UploadFileRequest]) (*connect.Response[v1.UploadFileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spalt.v1.FilesService.UploadFile is not implemented"))
}

func (UnimplementedFilesServiceHandler) GetFileURL(context.Context, *connect.Request[v1.GetFileURLRequest]) (*connect.Response[v1.GetFileURLResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spalt.v1.FilesService.GetFileURL is not implemented"))
}

func (UnimplementedFilesServiceHandler) ListFiles(context.Context, *connect.Request[v1.ListFilesRequest]) (*connect.Response[v1.ListFilesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("spalt.v1.FilesService.ListFiles is not implemented"))
}
