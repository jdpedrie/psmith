// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: spalt/v1/events.proto

package spaltv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/jdpedrie/spalt/gen/spalt/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EventsServiceName is the fully-qualified name of the EventsService service.
	EventsServiceName = "spalt.v1.EventsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EventsServiceSubscribeAccountEventsProcedure is the fully-qualified name of the EventsService's
	// SubscribeAccountEvents RPC.
	EventsServiceSubscribeAccountEventsProcedure = "/spalt.v1.EventsService/SubscribeAccountEvents"
)

// EventsServiceClient is a client for the spalt.v1.EventsService service.
type EventsServiceClient interface {
	// Open a long-lived server-streaming subscription for the calling
	// user's account events. The server pushes events as they fire;
	// the client closes the stream when it goes to sleep / detaches.
	// No replay — events are fire-and-forget; reconnection starts a
	// fresh subscription.
	SubscribeAccountEvents(context.Context, *connect.Request[v1.SubscribeAccountEventsRequest]) (*connect.ServerStreamForClient[v1.AccountEvent], error)
}

// NewEventsServiceClient constructs a client for the spalt.v1.EventsService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEventsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EventsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	eventsServiceMethods := v1.File_spalt_v1_events_proto.Services().ByName("EventsService").Methods()
	return &eventsServiceClient{
		subscribeAccountEvents: connect.NewClient[v1.SubscribeAccountEventsRequest, v1.AccountEvent](
			httpClient,
			baseURL+EventsServiceSubscribeAccountEventsProcedure,
			connect.WithSchema(eventsServiceMethods.ByName("SubscribeAccountEvents")),
			connect.WithClientOptions(opts...),
		),
	}
}

// eventsServiceClient implements EventsServiceClient.
type eventsServiceClient struct {
	subscribeAccountEvents *connect.Client[v1.SubscribeAccountEventsRequest, v1.AccountEvent]
}

// SubscribeAccountEvents calls spalt.v1.EventsService.SubscribeAccountEvents.
func (c *eventsServiceClient) SubscribeAccountEvents(ctx context.Context, req *connect.Request[v1.SubscribeAccountEventsRequest]) (*connect.ServerStreamForClient[v1.AccountEvent], error) {
	return c.subscribeAccountEvents.CallServerStream(ctx, req)
}

// EventsServiceHandler is an implementation of the spalt.v1.EventsService service.
type EventsServiceHandler interface {
	// Open a long-lived server-streaming subscription for the calling
	// user's account events. The server pushes events as they fire;
	// the client closes the stream when it goes to sleep / detaches.
	// No replay — events are fire-and-forget; reconnection starts a
	// fresh subscription.
	SubscribeAccountEvents(context.Context, *connect.Request[v1.SubscribeAccountEventsRequest], *connect.ServerStream[v1.AccountEvent]) error
}

// NewEventsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEventsServiceHandler(svc EventsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	eventsServiceMethods := v1.File_spalt_v1_events_proto.Services().ByName("EventsService").Methods()
	eventsServiceSubscribeAccountEventsHandler := connect.NewServerStreamHandler(
		EventsServiceSubscribeAccountEventsProcedure,
		svc.SubscribeAccountEvents,
		connect.WithSchema(eventsServiceMethods.ByName("SubscribeAccountEvents")),
		connect.WithHandlerOptions(opts...),
	)
	return "/spalt.v1.EventsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EventsServiceSubscribeAccountEventsProcedure:
			eventsServiceSubscribeAccountEventsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEventsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEventsServiceHandler struct{}

func (UnimplementedEventsServiceHandler) SubscribeAccountEvents(context.Context, *connect.Request[v1.SubscribeAccountEventsRequest], *connect.ServerStream[v1.AccountEvent]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("spalt.v1.EventsService.SubscribeAccountEvents is not implemented"))
}
