/bin/
*.test
*.out
.env
.env.local

# `go build ./cmd/reeved` output + the file storage root that
# defaults to ./reeved-data when REEVE_DATA_DIR is unset.
/reeved
/reeved-data/

# IDE
.idea/
.vscode/
*.swp

# macOS
.DS_Store

# Per-user Claude Code state — user-level settings + scheduled-task locks
# don't belong in the repo.
.claude/

clients/ReeveSwift/.build
clients/ReeveSwift/.swiftpm
clients/reeved-mac/.build
clients/reeved-mac/.swiftpm
clients/reeved-ios/.build
clients/reeved-ios/.swiftpm
# Generated by `make ios-project` (xcodegen). project.yml is the source of truth.
clients/reeved-ios/ReeveiOS.xcodeproj