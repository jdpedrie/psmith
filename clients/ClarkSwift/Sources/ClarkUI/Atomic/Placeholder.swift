import SwiftUI

/// Placeholder so the empty ClarkUI target can compile while we scaffold real
/// shared atomics (MessageBubble, ChunkRenderer, etc).
public enum ClarkUI {
    public static let version = "0.1.0"
}
