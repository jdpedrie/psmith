import SwiftUI

/// Placeholder so the empty ReeveUI target can compile while we scaffold real
/// shared atomics (MessageBubble, ChunkRenderer, etc).
public enum ReeveUI {
    public static let version = "0.1.0"
}
