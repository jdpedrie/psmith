import SwiftUI

/// Placeholder so the empty SpaltUI target can compile while we scaffold real
/// shared atomics (MessageBubble, ChunkRenderer, etc).
public enum SpaltUI {
    public static let version = "0.1.0"
}
