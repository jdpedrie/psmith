import SwiftUI

/// Placeholder so the empty PsmithUI target can compile while we scaffold real
/// shared atomics (MessageBubble, ChunkRenderer, etc).
public enum PsmithUI {
    public static let version = "0.1.0"
}
