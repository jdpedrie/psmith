// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: message_attachments.sql

package store

import (
	"context"

	"github.com/google/uuid"
)

const attachFileToMessage = `-- name: AttachFileToMessage :one
INSERT INTO message_attachments (
    message_id, ordinal, file_id, kind, role_hint
) VALUES (
    $1, $2, $3, $4, $5
)
RETURNING message_id, ordinal, file_id, kind, role_hint
`

type AttachFileToMessageParams struct {
	MessageID uuid.UUID
	Ordinal   int32
	FileID    uuid.UUID
	Kind      string
	RoleHint  string
}

func (q *Queries) AttachFileToMessage(ctx context.Context, arg AttachFileToMessageParams) (MessageAttachment, error) {
	row := q.db.QueryRow(ctx, attachFileToMessage,
		arg.MessageID,
		arg.Ordinal,
		arg.FileID,
		arg.Kind,
		arg.RoleHint,
	)
	var i MessageAttachment
	err := row.Scan(
		&i.MessageID,
		&i.Ordinal,
		&i.FileID,
		&i.Kind,
		&i.RoleHint,
	)
	return i, err
}

const listAttachmentsForMessage = `-- name: ListAttachmentsForMessage :many
SELECT
    a.message_id, a.ordinal, a.file_id, a.kind, a.role_hint,
    f.sha256, f.mime_type, f.size_bytes, f.original_filename
FROM message_attachments a
JOIN files f ON f.id = a.file_id
WHERE a.message_id = $1
ORDER BY a.ordinal ASC
`

type ListAttachmentsForMessageRow struct {
	MessageID        uuid.UUID
	Ordinal          int32
	FileID           uuid.UUID
	Kind             string
	RoleHint         string
	Sha256           string
	MimeType         string
	SizeBytes        int64
	OriginalFilename *string
}

func (q *Queries) ListAttachmentsForMessage(ctx context.Context, messageID uuid.UUID) ([]ListAttachmentsForMessageRow, error) {
	rows, err := q.db.Query(ctx, listAttachmentsForMessage, messageID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListAttachmentsForMessageRow
	for rows.Next() {
		var i ListAttachmentsForMessageRow
		if err := rows.Scan(
			&i.MessageID,
			&i.Ordinal,
			&i.FileID,
			&i.Kind,
			&i.RoleHint,
			&i.Sha256,
			&i.MimeType,
			&i.SizeBytes,
			&i.OriginalFilename,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAttachmentsForMessages = `-- name: ListAttachmentsForMessages :many
SELECT
    a.message_id, a.ordinal, a.file_id, a.kind, a.role_hint,
    f.sha256, f.mime_type, f.size_bytes, f.original_filename
FROM message_attachments a
JOIN files f ON f.id = a.file_id
WHERE a.message_id = ANY($1::uuid[])
ORDER BY a.message_id, a.ordinal ASC
`

type ListAttachmentsForMessagesRow struct {
	MessageID        uuid.UUID
	Ordinal          int32
	FileID           uuid.UUID
	Kind             string
	RoleHint         string
	Sha256           string
	MimeType         string
	SizeBytes        int64
	OriginalFilename *string
}

// Bulk variant for history builder: one query per chain instead of N.
func (q *Queries) ListAttachmentsForMessages(ctx context.Context, dollar_1 []uuid.UUID) ([]ListAttachmentsForMessagesRow, error) {
	rows, err := q.db.Query(ctx, listAttachmentsForMessages, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListAttachmentsForMessagesRow
	for rows.Next() {
		var i ListAttachmentsForMessagesRow
		if err := rows.Scan(
			&i.MessageID,
			&i.Ordinal,
			&i.FileID,
			&i.Kind,
			&i.RoleHint,
			&i.Sha256,
			&i.MimeType,
			&i.SizeBytes,
			&i.OriginalFilename,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
